spring:
  application:
    name: gateway
  profiles:
    active: application
  cloud:
    config:
      uri: http://localhost:8762
      fail-fast: true

    gateway:
      discovery:
        locator:
          enabled: true
      default-filters:
        - CircuitBreaker