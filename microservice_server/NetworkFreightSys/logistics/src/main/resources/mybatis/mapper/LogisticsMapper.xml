<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.auto_warehouse.mapper.LogisticsMapper">

    <insert id="addLogistics" parameterType="com.example.auto_warehouse.bean.Logistics">
        insert into myscoremanage.logistics (oid, recordTime, location)
        values(#{oid}, #{recordTime}, #{location});
    </insert>
    <select id="showAllLogisticsByOid" resultType="com.example.auto_warehouse.bean.Logistics">
        select * from myscoremanage.logistics where oid=#{oid} order by recordTime desc
    </select>
</mapper>